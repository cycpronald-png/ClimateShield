---
phase: 07-data-integrity-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - V11-04

must_haves:
  truths:
    - "TypeScript compiles 0 errors"
    - "Vite build succeeds"
    - "All backend tests pass (including V11 data integrity tests)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with Phase 7 plan details"
      contains: "07-01-PLAN.md"
  key_links:
    - from: "npm run build"
      to: "src/"
      via: "TypeScript compilation + Vite bundling"
      pattern: "tsc -b && vite build"
    - from: "python3 -m pytest"
      to: "backend/tests/"
      via: "pytest test discovery"
      pattern: "pytest tests/"
---

<objective>
Run the full end-to-end verification suite proving all v1.1 changes work together. This is the final gate: TypeScript clean, Vite build clean, all backend tests pass (including the new V11 data integrity tests from Plan 01), and the ROADMAP is updated.

Purpose: V11-04 requires that TypeScript compiles 0 errors, Vite build succeeds, and all existing backend tests pass. This plan is the authoritative verification that the entire v1.1 milestone is complete.

Output: Updated ROADMAP.md with Phase 7 plan details + verification evidence
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-integrity-verification/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full V11-04 verification + update ROADMAP</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
Run the complete V11-04 verification sequence:

**Step 1: TypeScript type check**
```bash
npx tsc -b --noEmit
```
Must exit with code 0 and report 0 errors. If errors remain, go back to Plan 01 — the fixes were insufficient.

**Step 2: Vite production build**
```bash
npm run build
```
Must exit with code 0. The build script runs `tsc -b && vite build`.

**Step 3: All backend tests**
```bash
cd backend && python3 -m pytest tests/ -v
```
Must show ALL tests PASSED (0 failed). Count should be 71+ tests (existing) plus ~15+ new from test_data_integrity.py.

**Step 4: Specific V11 data integrity tests**
```bash
cd backend && python3 -m pytest tests/test_data_integrity.py -v
```
Verify all V11-01, V11-02, V11-03 tests pass.

**Step 5: Verify existing tests not regressed**
```bash
cd backend && python3 -m pytest tests/test_pipeline_resilience.py tests/test_api_crs_resilience.py tests/test_vocabulary.py tests/test_risk_config_sensitivity.py -v
```
All must pass — confirming no regression from Phase 7 changes.

**Step 6: Update ROADMAP.md**

Find the Phase 7 section and update:

Change:
```
**Plans**: TBD
```
To:
```
**Plans**: 2 plans

Plans:
- [ ] 07-01-PLAN.md — Fix TypeScript build errors + V11-01/02/03 integration tests
- [ ] 07-02-PLAN.md — Full V11-04 end-to-end verification + ROADMAP update
```

Update Phase 7 status row in the progress table from `0/? | Not started` to reflect planned status.

**Step 7: Record results**

If all verifications pass, Phase 7 is complete. The project state should be updated to reflect Phase 7 completion.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && npm run build && cd backend && python3 -m pytest tests/ -q</automated>
  </verify>
  <done>
    - TypeScript 0 errors confirmed
    - Vite build succeeds
    - All backend tests pass (71+ original + V11 data integrity tests)
    - No regressions in any existing test file
    - ROADMAP.md updated with Phase 7 plan details
  </done>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits 0
    - `npm run build` exits 0
    - `cd backend && python3 -m pytest tests/ -q` exits 0 with all X passed
    - `.planning/ROADMAP.md` contains `07-01-PLAN.md`
    - `.planning/ROADMAP.md` contains `07-02-PLAN.md`
    - `.planning/ROADMAP.md` Phase 7 has `2 plans` listed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This is a verification-only plan — no code changes beyond documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | I | Build output | accept | Build verification is read-only, no integrity risk |

</threat_model>

<verification>
1. `npx tsc -b --noEmit` — 0 errors
2. `npm run build` — exits 0
3. `cd backend && python3 -m pytest tests/ -v` — all tests pass
4. `cd backend && python3 -m pytest tests/test_data_integrity.py -v` — all V11 tests pass
5. ROADMAP.md updated with plan details
</verification>

<success_criteria>
- TypeScript 0 errors
- Vite build succeeds
- All backend tests pass
- ROADMAP.md has Phase 7 plan list
- Phase 7 verification complete — v1.1 milestone fully verified
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-integrity-verification/07-02-SUMMARY.md`
</output>