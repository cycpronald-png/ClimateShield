---
phase: 05-vocabulary-unification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/services/climate/vocabulary.py
  - backend/tests/test_vocabulary.py
autonomous: true
requirements:
  - VOCAB-01

must_haves:
  truths:
    - "normalize_risk_level('Critical') returns 'Purple'"
    - "normalize_risk_level('High') returns 'Red'"
    - "normalize_risk_level('Moderate') returns 'Yellow'"
    - "normalize_risk_level('Low') returns 'Safe'"
    - "normalize_risk_level('Purple') returns 'Purple' (v2 pass-through)"
    - "normalize_risk_level(None) returns 'Safe'"
    - "RiskState enum has exactly 5 members: Safe, Low, Yellow, Red, Purple"
  artifacts:
    - path: "backend/services/climate/vocabulary.py"
      provides: "RiskState enum, V1_TO_V2 mapping, normalize_risk_level()"
      exports: ["RiskState", "V1_TO_V2", "normalize_risk_level"]
      min_lines: 30
    - path: "backend/tests/test_vocabulary.py"
      provides: "Tests for vocabulary module"
      contains: "test_normalize_risk_level"
  key_links:
    - from: "backend/services/climate/vocabulary.py"
      to: "backend/services/climate/scoring_v2.py"
      via: "lookup_state pattern reference (same state names)"
      pattern: "Safe.*Low.*Yellow.*Red.*Purple"
---

<objective>
Create the canonical vocabulary module with RiskState enum, V1→V2 mapping, and normalize_risk_level() function.

Purpose: Establish the single source of truth for risk state names across the entire backend. All other code will import from this module instead of defining their own vocabulary (per D-01, D-02, D-03, D-04).
Output: `vocabulary.py` with RiskState enum, V1_TO_V2 dict, and normalize_risk_level(); comprehensive test suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vocabulary-unification/05-CONTEXT.md

<interfaces>
<!-- Existing v2 state system the new vocabulary must match -->

From backend/services/climate/scoring_v2.py:
```python
def lookup_state(risk_score: float, state_ranges: List[Dict[str, Any]]) -> str:
    """Uses priority order: Purple, Red, Yellow, Low, Safe"""
    priority_order = ["Purple", "Red", "Yellow", "Low", "Safe"]
```

From backend/services/risk_config_service.py:
```python
DEFAULT_CONFIG = {
    "state_ranges": [
        {"name": "Safe", "min": 0, "max": 12},
        {"name": "Low", "min": 13, "max": 16},
        {"name": "Yellow", "min": 17, "max": 22},
        {"name": "Red", "min": 23, "max": 26},
        {"name": "Purple", "min": 25, "max": 30},
    ],
}
```
</interfaces>
</context>

<feature>
  <name>Canonical RiskState Vocabulary</name>
  <files>backend/services/climate/vocabulary.py, backend/tests/test_vocabulary.py</files>
  <behavior>
    - RiskState enum with members: Safe, Low, Yellow, Red, Purple (using auto() values per D-01)
    - V1_TO_V2 mapping dict: Critical→Purple, High→Red, Moderate→Yellow, Low→Safe (per D-03)
    - normalize_risk_level("Critical") == "Purple"
    - normalize_risk_level("High") == "Red"
    - normalize_risk_level("Moderate") == "Yellow"
    - normalize_risk_level("Low") == "Safe"
    - normalize_risk_level("Purple") == "Purple" (v2 pass-through)
    - normalize_risk_level("Safe") == "Safe" (v2 pass-through)
    - normalize_risk_level("Yellow") == "Yellow" (v2 pass-through)
    - normalize_risk_level("Red") == "Red" (v2 pass-through)
    - normalize_risk_level(None) == "Safe" (safe default per D-04)
    - normalize_risk_level("unknown_string") == "Safe" (safe default per D-04)
    - DataUnavailable is NOT an enum member (per D-02 — display concern only)
  </behavior>
  <implementation>
    Create vocabulary.py with:
    1. `from enum import Enum, auto`
    2. `class RiskState(Enum)` with Safe/Low/Yellow/Red/Purple using `auto()`
    3. `V1_TO_V2` dict mapping 4 v1 terms to v2 equivalents
    4. `normalize_risk_level(level: str | None) -> str` that lowercases input, checks V1_TO_V2 first, then checks if input is a valid v2 state name (case-insensitive), returns "Safe" for unknown/None
    5. Module docstring explaining: canonical vocabulary, V1→V2 mapping, no DB migration needed (read-time conversion)
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → application | Legacy v1 risk_level strings in DB rows are untrusted input to normalize_risk_level() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | S (Spoofing) | normalize_risk_level() | mitigate | Unknown/None input defaults to "Safe" — cannot spoof a higher risk level from crafted DB strings |
| T-05-02 | T (Tampering) | V1_TO_V2 dict | accept | Static dict, not user-modifiable; low-value target |
</threat_model>

<verification>
```bash
cd backend && python -m pytest tests/test_vocabulary.py -v
```
All tests pass: enum membership, V1→V2 mapping, v2 pass-through, None/unknown defaults.
</verification>

<success_criteria>
1. `backend/services/climate/vocabulary.py` exists with RiskState enum, V1_TO_V2 dict, normalize_risk_level() function
2. `backend/tests/test_vocabulary.py` passes all test cases
3. RiskState has exactly 5 members matching v2 state names
4. normalize_risk_level correctly maps all 4 v1 terms, passes through v2 terms, defaults unknown/None to "Safe"
5. No "DataUnavailable" member in the enum
</success_criteria>

<output>
After completion, create `.planning/phases/05-vocabulary-unification/05-01-SUMMARY.md`
</output>